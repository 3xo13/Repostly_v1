export const discriptionPage = async (page, discription) => {
	const inputSelector = 'textarea';
	const submitBtnSelector = 'form button[type="submit"]'
	try {

		return true;
	} catch (error) {
		console.log("🚀 ~ discriptionPage ~ error:", error)
		return false;
	}
}